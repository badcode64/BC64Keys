.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc

# App build artifacts produced by ./build.sh (only in project root)
/BC64Keys
/BC64Keys_tmp
/BC64Keys.app/
/BC64Keys.dSYM/

# Local logs
/tmp/bc64keys-status.log

# Editor / misc
.vscode/
*.swp

# Local manual backups (these should not be committed)
*.bak
*.backup-*
Sources.backup/

# Apple Developer Certificates & Keys (security - never commit these)
*.p12
*.key
*.pem
*.cer
*.certSigningRequest
*.mobileprovision
*.provisionprofile
*_key.pem
Certificates/
Keys/

# Notarization credentials (CRITICAL - never commit)
*.credentials
*.password
notarization-password.txt
apple-credentials.txt
.notarization-password
.notarization-credentials.sh
.apple-id
*.keychain
exportOptions.plist

# Build artifacts with embedded credentials
*.xcarchive
*.pkg
*.dmg
*.zip.dSYM

# Environment files
.env
.env.local
secrets.sh

# Notarization scripts (keep local only)
notarize.sh
NOTARIZATION_GUIDE.md
